<h1>Post Search</h1>

<%= form_tag(search_path, method: 'get') do %>
  <%= text_field_tag(:q, params[:q]) %>
  <%= submit_tag('Search') %>
<% end %>

<h1>Results</h1>
<ul>
  <% @search.each do |post| %>
    <li>
      <%= link_to post.title, post_path(post._id) %>
    </li>
  <% end %>
</ul>